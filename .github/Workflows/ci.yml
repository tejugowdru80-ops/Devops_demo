name: Simple CI Pipeline

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest


  steps:
    - name: Checkout repository
      uses: actions/Checkout@v3

    - name: Run a simple command
      run: echo "I need to become good Bioinformatician."